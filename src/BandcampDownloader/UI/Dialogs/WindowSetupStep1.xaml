<Window
    x:Class="BandcampDownloader.UI.Dialogs.WindowSetupStep1"
    x:ClassModifier="internal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:settings="clr-namespace:BandcampDownloader.Settings"
    Title="Setup - Step 1/2"
    Width="400"
    Height="350"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,20"
            FontSize="20"
            FontWeight="Bold"
            Text="Welcome to Bandcamp Downloader"
            TextAlignment="Center" />

        <TextBlock
            Grid.Row="1"
            Margin="0,0,0,20"
            Text="Step 1/2: Choose your language and theme"
            TextAlignment="Center"
            TextWrapping="Wrap" />

        <!-- Language Selection -->
        <TextBlock
            Grid.Row="2"
            Margin="0,0,0,5"
            FontWeight="Bold"
            Text="Language:" />

        <ComboBox
            x:Name="ComboBoxLanguage"
            Grid.Row="3"
            Margin="0,0,0,15"
            Height="30" />

        <!-- Theme Selection -->
        <TextBlock
            Grid.Row="4"
            Margin="0,0,0,5"
            FontWeight="Bold"
            Text="Theme:" />

        <ComboBox
            x:Name="ComboBoxTheme"
            Grid.Row="5"
            Margin="0,0,0,15"
            Height="30"
            SelectionChanged="ComboBoxTheme_SelectionChanged" />

        <!-- Buttons -->
        <StackPanel
            Grid.Row="6"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="100"
                Height="30"
                Margin="0,0,10,0"
                Click="ButtonCancel_Click"
                Content="Cancel" />
            <Button
                Width="100"
                Height="30"
                Click="ButtonNext_Click"
                Content="Next" />
        </StackPanel>
    </Grid>
</Window>
